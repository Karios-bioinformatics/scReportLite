% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_qc.R
\name{build_qc_payload}
\alias{build_qc_payload}
\title{Build QC data payload for client-side rendering}
\usage{
build_qc_payload(
  qc_df,
  cluster_col = "cluster",
  cell_col = "cell",
  sample_col = "sample",
  max_points_per_group = 1000
)
}
\arguments{
\item{qc_df}{Data frame with QC metrics.}

\item{cluster_col}{Name of the cluster column (default "cluster").}

\item{cell_col}{Name of the cell/barcode column.}

\item{sample_col}{Name of the sample column.}

\item{max_points_per_group}{Cap on jitter-point rows per sample×metric group
sent to the browser.  Violin KDE uses full data; only the overlay points
are sampled.  Default 1000.}
}
\value{
A named list with elements \code{samples}, \code{sample_colors},
  \code{cells} (a list of per-cell records).
}
\description{
Returns a list ready for \code{jsonlite::toJSON(..., auto_unbox = TRUE)}
containing sample metadata and per-cell QC metrics.  No plotly htmlwidgets
are created — all rendering happens in the browser on a single active canvas.
}
\keyword{internal}
