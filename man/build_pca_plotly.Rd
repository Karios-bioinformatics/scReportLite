% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_pca.R
\name{build_pca_plotly}
\alias{build_pca_plotly}
\title{Build an interactive plotly PCA scatter plot}
\usage{
build_pca_plotly(
  pca_df,
  cluster_col,
  cell_col,
  sample_col = NULL,
  color_by = "cluster",
  point_size,
  point_alpha,
  use_webgl = TRUE
)
}
\arguments{
\item{pca_df}{Data frame with PCA coordinates and cluster assignments.
Must contain columns: \code{cell}, \code{PC_1}, \code{PC_2}, \code{cluster}.}

\item{cluster_col}{Name of the cluster column in pca_df}

\item{cell_col}{Name of the cell/barcode column in pca_df}

\item{sample_col}{Optional name of the sample column (NULL to skip)}

\item{color_by}{Column name to colour by. Default \code{"cluster"}.
Must exist in \code{pca_df}; falls back to \code{"cluster"} with a
warning if missing.}

\item{point_size}{Marker size for scatter points}

\item{point_alpha}{Opacity for markers (0-1)}

\item{use_webgl}{Use WebGL (scattergl) instead of SVG (scatter).
Default: \code{TRUE}.}
}
\value{
A plotly htmlwidget object
}
\description{
Creates a plotly scatter plot with one trace per unique value of the
colour-grouping column, mirroring the UMAP build architecture.
\code{customdata} is a 5-element array for each cell:
\code{[cell_id, cluster, sample, PC_1, PC_2]}.
Hover text always includes the original cluster assignment regardless
of the colour-grouping column.
}
\keyword{internal}
